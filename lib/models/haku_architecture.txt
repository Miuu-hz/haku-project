┌─────────────────────────────────────────────────────────────────────────────┐
│                           HAKU AI SYSTEM (current)                           │
│                     Model: Gemma 4 E2B/E4B via LiteRT-LM                    │
│                     Context: 4K–8K tokens (no lean syntax needed)            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                      REAL-TIME LAYER (ขณะแชท)                          │ │
│  │                                                                         │ │
│  │  ① SmartPreprocessor (rule-based, 0 LLM tokens)                        │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐   │ │
│  │  │ User message (Thai/EN)                                           │   │ │
│  │  │  → FactWorker / CalendarWorker / HealthDoctor (0 tok, regex)    │   │ │
│  │  │  → PATH A: rule matched → handle directly (search/calendar/…)  │   │ │
│  │  │  → PATH B: intent=general → ไปขั้นถัดไป                          │   │ │
│  │  └─────────────────────────────────────────────────────────────────┘   │ │
│  │                              │ (PATH B only)                            │ │
│  │  ② TagContextService (0 LLM — keyword + SQL search)                    │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐   │ │
│  │  │ keyword extraction จาก userMessage                              │   │ │
│  │  │ → SQL search บน english_chat_log + diary entries               │   │ │
│  │  │ → ถ้าเจอ → prepend "Past related:\n[d/m] @loc ..." ใน context  │   │ │
│  │  └─────────────────────────────────────────────────────────────────┘   │ │
│  │                              │                                          │ │
│  │  ③ Calendar Context (0 LLM — keyword-based)                            │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐   │ │
│  │  │ _isScheduleQuery(userMessage) → keyword check                   │   │ │
│  │  │ ถ้า true → inject today's events จาก Google Calendar            │   │ │
│  │  └─────────────────────────────────────────────────────────────────┘   │ │
│  │                              │                                          │ │
│  │  ④ Stage 1: The Face (ตอบ user ทันที)                                  │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐   │ │
│  │  │ Gemma 4 E2B/E4B (LiteRT-LM) — ตอบภาษาไทยธรรมชาติ               │   │ │
│  │  │ Input: context (tag history + calendar) + user message          │   │ │
│  │  │ Output: Thai response → แสดง user ทันที                         │   │ │
│  │  │ Context budget: 1,500–3,000 chars (4K–8K token window)          │   │ │
│  │  └─────────────────────────────────────────────────────────────────┘   │ │
│  │                              │                                          │ │
│  │  ⑤ Secret Chat (async หลัง Face ตอบ — ไม่ block UI)                    │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐   │ │
│  │  │ Input: Thai exchange (user msg + AI response)                   │   │ │
│  │  │ LLM: buildWorkerExtractPrompt() → JSON extraction               │   │ │
│  │  │ Output: EnglishLogEntry {                                       │   │ │
│  │  │   summaryEn: "User went to beach with partner"                  │   │ │
│  │  │   intent: log | schedule | query | chat                        │   │ │
│  │  │   tags: ["beach", "partner"]                                    │   │ │
│  │  │   location: "beach", mood: 8                                    │   │ │
│  │  │ }                                                               │   │ │
│  │  │ → Store in SharedPreferences (english_chat_log, 50 entries)    │   │ │
│  │  │ → TagContextService.saveTagsToRecentEntry() → merge to SQLite  │   │ │
│  │  └─────────────────────────────────────────────────────────────────┘   │ │
│  │                              │                                          │ │
│  │  ⑥ Big Manager (async — dispatch from English log)                      │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐   │ │
│  │  │ Reads EnglishLogEntry (no LLM call — intent already extracted)  │   │ │
│  │  │ dispatch:                                                        │   │ │
│  │  │   intent=schedule → CalendarWorker.addEvent()                   │   │ │
│  │  │   intent=log      → FactWorker.extractFacts()                   │   │ │
│  │  │   intent=search   → WebSearchService.search()                   │   │ │
│  │  │   intent=chat     → (no action, logged only)                    │   │ │
│  │  └─────────────────────────────────────────────────────────────────┘   │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  TOKEN BUDGET (per exchange):                                                │
│  ④ Face LLM:     1 call  (ตอบ user)                                         │
│  ⑤ Secret Chat:  1 call  (extract → English log, async)                     │
│  ⑥ Big Manager:  0 calls (อ่าน intent จาก Secret Chat โดยตรง)               │
│  ─────────────────────────────────────────────────────                      │
│  Total: 2 calls/exchange (ลดจาก 2-3 เพราะลบ PreClassify ออกแล้ว)            │
│                                                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                      SEARCH STRATEGY                                    │ │
│  │                                                                         │ │
│  │  Fast Path (ทันที, 0 LLM):                                              │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐   │ │
│  │  │ SmartPreprocessor → keyword extraction (regex)                  │   │ │
│  │  │ → SQL LIKE search on raw Thai entries                           │   │ │
│  │  │ → ถ้าเจอ → เพิ่มใน context ก่อนส่ง Face                         │   │ │
│  │  └─────────────────────────────────────────────────────────────────┘   │ │
│  │                                                                         │ │
│  │  Tag Context (0 LLM — TagContextService):                               │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐   │ │
│  │  │ keyword → search english_chat_log + SQLite entries              │   │ │
│  │  │ → "Past related:\n[5/1] @beach User went to beach"              │   │ │
│  │  │ → prepend ใน context window (Gemma 4 รองรับ 4K+ token)          │   │ │
│  │  └─────────────────────────────────────────────────────────────────┘   │ │
│  │                                                                         │ │
│  │  Slow Path (future, LLM-assisted):                                      │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐   │ │
│  │  │ ถ้า Fast Path ไม่เจอ → Vector search English DB (sqlite-vec)    │   │ │
│  │  │ → ส่ง context ให้ Face ตอบ                                       │   │ │
│  │  └─────────────────────────────────────────────────────────────────┘   │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                      TRIGGER LAYER (Event-based)                        │ │
│  │                                                                         │ │
│  │   ⏰ TimerTrigger              📍 LocationTrigger                       │ │
│  │   • 30 นาทีหลังถึงร้าน          • ถึงที่ทำงาน                            │ │
│  │   • "ร้านนี้เป็นไงบ้าง?"        • ถึงบ้าน                                │ │
│  │                                                                         │ │
│  │   🔋 ChargingTrigger           🌅 MorningTrigger (6:00)                 │ │
│  │   • เสียบชาร์จ = จบวัน          • แจ้งเตือนเช้า                          │ │
│  │   • เริ่ม ManagerSummary       • สุขภาพ + ตารางวัน                       │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                 BACKGROUND LAYER (จบวัน/ตอนชาร์จ)                       │ │
│  │                                                                         │ │
│  │   Input: English chat logs (from Secret Chat) +                         │ │
│  │          English diary translations (from TranslatorWorker)             │ │
│  │                                                                         │ │
│  │   ┌─────────────────────────────────────────────────────────────────┐  │ │
│  │   │              ManagerSummaryStrategy (Orchestrator)              │  │ │
│  │   │                                                                  │  │ │
│  │   │  1. รวม English logs ทั้งวัน                                     │  │ │
│  │   │  2. วิเคราะห์ pattern + extract insights (จาก VectorService)    │  │ │
│  │   │  3. Dispatch ไปยัง Specialists                                   │  │ │
│  │   └─────────────────────────────────────────────────────────────────┘  │ │
│  │                              │                                          │ │
│  │           ┌──────────────────┼──────────────────┐                       │ │
│  │           ▼                  ▼                  ▼                       │ │
│  │   ┌─────────────┐   ┌─────────────┐   ┌─────────────┐                  │ │
│  │   │HealthDoctor │   │FactWorker   │   │CalendarWork │                  │ │
│  │   │             │   │             │   │             │                  │ │
│  │   │ • period    │   │ • fav place │   │ • predict   │                  │ │
│  │   │ • symptoms  │   │ • habits    │   │   events    │                  │ │
│  │   │ • track 2-5d│   │ • facts     │   │   2-5 days  │                  │ │
│  │   └─────────────┘   └─────────────┘   └─────────────┘                  │ │
│  │          │                 │                  │                         │ │
│  │          ▼                 ▼                  ▼                         │ │
│  │   ┌─────────────────────────────────────────────────┐                  │ │
│  │   │           💾 RAG Storage (SQLite, English)       │                  │ │
│  │   │  • health_facts    • favorite_places             │                  │ │
│  │   │  • preferences     • scheduled_reminders         │                  │ │
│  │   │  • english_chat_log (from Secret Chat)           │                  │ │
│  │   └─────────────────────────────────────────────────┘                  │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘


─────────────────────────────────────────────────────────────┐
│                   SmartPreprocessor (PATH B)                 │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   User Message: "นัดหมอพรุ่งนี้ ฉันเป็นเมนด้วย"                │
│                         │                                    │
│        ┌────────────────┼────────────────┐                  │
│        │                │                │                  │
│        ▼                ▼                ▼                  │
│   ┌─────────┐    ┌───────────┐    ┌────────────┐           │
│   │FactWork │    │CalendarWk │    │HealthDoc   │           │
│   │ 0 tok   │    │ 0 tok     │    │ 0 tok      │           │
│   └─────────┘    └───────────┘    └────────────┘           │
│        │                │                │                  │
│        ▼                ▼                ▼                  │
│   ┌─────────────────────────────────────────────────────┐  │
│   │              WorkerResults                          │  │
│   │ facts: [], events: [หมอ], health: [period]          │  │
│   └─────────────────────────────────────────────────────┘  │
│                         │                                    │
│        ┌────────────────┘                                    │
│        ▼                                                     │
│   [TagContextService] keyword search on english_chat_log    │
│   → "Past related:\n[3/1] @clinic User visited doctor..."   │
│                         │                                    │
│   [Calendar Context] _isScheduleQuery("นัดหมอ") → true       │
│   → inject today/tomorrow events from Google Calendar       │
│                         │                                    │
│                         ▼                                    │
│   ┌─────────────────────────────────────────────────────┐  │
│   │              Context Window (Gemma 4)                │  │
│   │ [Profile: name=ฟุกิ][Health:Period][Cal:หมอ,พรุ่งนี้] │  │
│   │ [Past related] 3/1 @clinic "ไปหาหมอ 3 เดือนก่อน"     │  │
│   │ [Calendar] พรุ่งนี้: หมอ 09:00                        │  │
│   └─────────────────────────────────────────────────────┘  │
│                         │                                    │
│                         ▼                                    │
│             Gemma 4 E2B/E4B (Face) — LiteRT-LM              │
│                         │                                    │
│                         ▼ (async, หลังตอบ user)              │
│               Secret Chat → EnglishLogEntry                  │
│               → Big Manager dispatch (0 LLM)                │
│                                                              │
└─────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                         TriggerService                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   ┌─────────────┐    ┌─────────────┐    ┌─────────────────────┐ │
│   │TimerTrigger │    │ChargingTrig │    │ManagerSummaryStrat  │ │
│   │             │    │             │    │                     │ │
│   │ • 30m ถึงร้าน │    │ • จบวัน     │    │ • วิเคราะห์ pattern │ │
│   │ • เช้า 6 โมง  │    │ • Morning   │    │ • Health insights  │ │
│   │ • Health    │    │ • Health    │    │ • Dispatch workers  │ │
│   └─────────────┘    └─────────────┘    └─────────────────────┘ │
│                             │                                    │
│                             ▼                                    │
│                   ┌─────────────────────┐                       │
│                   │  UnifiedVectorService│                       │
│                   │  + UserProfileService│                       │
│                   │  (no LeanContext)    │                       │
│                   └─────────────────────┘                       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                  REMOVED (migration to Gemma 4)                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ✂️ LeanContextService     — lean syntax ไม่จำเป็นแล้ว           │
│     (lean Thai ~50 chars/msg → English ~15 chars/msg)           │
│     เหตุผล: Gemma 4 รองรับ 4K-8K token window                    │
│                                                                  │
│  ✂️ PreClassify step       — LLM call ก่อน Face                  │
│     (detect intent: schedule/query/search/log/chat)             │
│     เหตุผล: Gemma 4 เข้าใจ intent ได้เองโดยตรง                  │
│     แทนที่ด้วย: _isScheduleQuery() keyword check (0 LLM)         │
│                                                                  │
│  ✂️ PreClassifyResult class — ผล intent+summary จาก PreClassify  │
│  ✂️ buildPreClassifyPrompt() — prompt สำหรับ classify            │
│                                                                  │
│  Before: 2-3 LLM calls/exchange (PreClassify + Face + Secret)   │
│  After:  2 LLM calls/exchange   (Face + Secret)                 │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
